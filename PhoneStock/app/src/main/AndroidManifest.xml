<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.phonestock">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PhoneStock">
        <service
            android:name=".MyService3"
            android:enabled="true"
            android:exported="true">
        </service>

        <receiver
            android:name=".UpdateReceiver"
            android:enabled="true"
            android:exported="true" >
            <intent-filter>
                <action android:name="com.example.phonestock.UPDATE_PHONE" />
            </intent-filter>
        </receiver>

        <service
            android:name=".MyService2"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name=".MyService1"
            android:enabled="true"
            android:exported="true" />

        <receiver
            android:name=".DeleteAllReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="@string/delete_phone" />
            </intent-filter>
        </receiver>
        <receiver
            android:name=".PhoneReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="@string/add_phone" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".Setting"
            android:label="Setting"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Stock"
            android:label="Stock"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>